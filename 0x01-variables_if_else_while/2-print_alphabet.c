#include <stdio.h>

/**
 * main - Identifies whether n is positive or negative.
 *
 * Return: Always 0
 */

int main(void)
{
	char i = 97;

	for (i = 97; i <= 122; i++)
		putchar(i);
	putchar(10);
	return (0);
}
