#include <stdio.h>

/**
 * main - Identifies whether n is positive or negative.
 *
 * Return: Always 0
 */

int main(void)
{
	char i;

	for (i = 122; i >= 97; i--)
		putchar(i);
	putchar(10);
	return (0);
}
